using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
public class PurchaseButton : MonoBehaviour
{
    public enum PurchaseType {coins100, coins150, coins200, removeBannerAds, removeVideoAds, removeAllAds};
    public PurchaseType purchaseType;

    public void ClickPurchaseButton()
    {
        switch(purchaseType)
        {
            case PurchaseType.coins100:
                ShopItems.Instance.BuyCoins100();
                break;
            case PurchaseType.coins150:
                ShopItems.Instance.BuyCoins150();
                break;
            case PurchaseType.coins200:
                ShopItems.Instance.BuyCoins200();
                break;
            case PurchaseType.removeBannerAds:
                ShopItems.Instance.BuyRemoveBannerAds();
                break;
            case PurchaseType.removeVideoAds:
                ShopItems.Instance.BuyRemoveVideoAds();
                break;
            case PurchaseType.removeAllAds:
                ShopItems.Instance.BuyRemoveAllAds();
                break;
        }
    }
}
*/
